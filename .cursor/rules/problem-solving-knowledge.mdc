---
description: 排障与 bug 修复后强制写入 docs/problem-solving-log.md 沉淀
alwaysApply: true
---

# 问题解决沉淀（强制）

当本轮对话完成 **bug 修复、线上/生产问题排查、非琐碎的故障定位**（含认证、会话、部署、构建、数据与中间件等）时，**必须**在 `docs/problem-solving-log.md` **末尾追加一条**记录，不得省略。

## 条目要求

- 使用文件内 **写入模板** 结构：`### YYYY-MM-DD — 标题`，下接 `现象` / `根因` / `处理`，必要时加 `涉及文件`。
- 日期以用户上下文中的「今天」为准；无则使用仓库所在环境日期。
- 语言：**简体中文**，表述简洁、可检索。
- 若同一问题已有一条高度重复的条目，可改为 **补充** 现有条目一小段「补充（YYYY-MM-DD）」，避免复制粘贴式重复。

## 例外

仅当用户在同一条用户消息中 **明确写「本次不写沉淀 / 跳过记录」** 时，可跳过追加。
